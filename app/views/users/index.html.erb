<%= render 'navbar' %>

<!-- Titre principal de la page -->
<% if current_user.aupair? %>
  <h1 class="text-center my-4">Find a family</h1> <!-- Centré et avec marges verticales -->
<% else %>
  <h1 class="text-center my-4">Find your next au-pair</h1> <!-- Centré et avec marges verticales -->
<% end %>

<!-- Affichage des filtres -->
<%= render 'filters' %>

<!-- Grille pour afficher les cartes des aupairs -->
<%= render 'users_list' %>

<!-- Script java pour la gestion du filtre nombre d'enfants avec slider -->
<%= javascript_include_tag "controllers/users_index", defer: true %>
